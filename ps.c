
#include "types.h"
#include "stat.h"
#include "user.h"
#include "procstat.h"

int main(int argc, char *argv[]) {
    printStatus();
    // struct proc_stat *ps = (struct proc_stat *)malloc(sizeof(struct
    // proc_stat));
    // // ps;
    // int pid = 3;
    // getpinfo(ps, pid);
    // printf(1, "%d %d %d %d \n", ps->pid, ps->current_queue, ps->num_run,
    //        ps->runtime);
    // printf(1, "%d \n", pid);
    // printf(1, "%d \n", ps->);

    // getpinfo();
    exit();
}